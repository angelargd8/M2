let nums: integer[] = [1, 2, 3];

// verificación de tipo
// nums[0] = "one";   // ❌ error: string no compatible con integer

// validación de índices
let first: integer = nums[0];   // ✅ válido
// let invalid: integer = nums[3]; // ❌ error: índice fuera de rango
